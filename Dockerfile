FROM node:12

WORKDIR /usr/src/app

COPY yarn.lock .

RUN yarn

COPY . .

ARG REDIS_ENDPOINT_URI
ARG REDIS_PASSWORD

ARG REDIS_URI_ADDRESS

ENV REDIS_URI=$REDIS_URL_ADDRESS
ENV REDIS_PASSWORD=$REDIS_PASSWORD
ENV PORT 

CMD [ "node", "api/index.js" ]


