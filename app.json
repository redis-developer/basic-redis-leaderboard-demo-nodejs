{
  "name": "Basic Redis Leaderboard Demo NodeJS",
  "description": "List of top 100 companies",
  "repository": "https://github.com/",
  "logo": "https://redis.io/images/redis-white.png",
  "keywords": ["node", "express", "redis", "leadboard"],
  "env": {
		"REDIS_ENDPOINT_URI": {
	  	"description": "Redis server URI",
      "required": true
    },
    "REDIS_PASSWORD": {
	    "description": "Password to your Redis server",
	    "required": true
    }
  }
}